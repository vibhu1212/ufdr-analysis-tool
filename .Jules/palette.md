## 2024-04-13 - [Chat Empty State]
**Learning:** Conversational interfaces without an empty state cause a 'blank canvas' problem where users don't know what queries are supported. In this app, unified search must provide context about supported queries (e.g. contacts, messages, locations) since it spans multiple data types.
**Action:** Always provide an empty state with example queries for chat interfaces.
